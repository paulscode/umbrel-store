manifestVersion: 1.1
id: paulscode-knots-blake2b
implements:
  - bitcoin
category: bitcoin
name: Knots (BLAKE2b) Companion
version: "1.1.10"
tagline: A Bitcoin Knots node on the BLAKE2b chain, pruned so it fits alongside another
description: >-
  A Bitcoin Knots node built from the BLAKE2b proof-of-work change, following the
  BLAKE2b chain on mainnet. Install it alongside your existing Bitcoin node
  rather than in place of it: this one is pruned, so both chains fit on one
  server without room for two full blockchains.


  Bitcoin's mainnet split on 30 August 2026. The two chains part at block
  961632, and from block 961640 one of them uses BLAKE2b for proof of work
  instead of SHA256d. BLAKE2b is the algorithm Sia mines, so ASICs built for Sia
  can mine this chain. This app runs a node that follows it and enforces its
  rules.


  THIS IS A REAL CHAIN WITH REAL BLOCK REWARDS


  Blocks you mine here pay a real subsidy to a real address, and the chain has
  other participants. Both sides of the split claim to be Bitcoin; which one you
  follow is your decision, and installing this makes it.


  WHAT IT USES


  About 5 GB of blocks by default rather than the whole chain, plus the node's
  own data, which grows on its own. Change that under Advanced Settings, along
  with everything else the official Bitcoin Node app lets you change. The first
  sync downloads the chain from the network and takes a while.


  IT LOOKS AND WORKS LIKE THE OFFICIAL NODE APP


  Opening it gives you the same dashboard: sync progress, block and peer lists, a
  connect modal with your node's addresses and RPC credentials, and the full
  settings page. Two home-screen widgets as well. It is a fork of the app the
  official Bitcoin Knots app runs, with one thing changed, which is which
  bitcoind it starts.


  To mine this chain, install Datum (BLAKE2b) Companion as well.
developer: Paul Lamb
website: https://github.com/paulscode/knots-blake2b-startos
dependencies: []
repo: https://github.com/paulscode/umbrel-bitcoin-blake2b
support: https://paulscode.com/c/bitcoin/8
# Not in the 3000s, where the app tiles crowd together: 3028 was already taken by
# paulscode-agent-wallet on the author's own box, and a tile port that collides
# fails the whole install with "port is already allocated". 7150 sits beside the
# gateway app's 7153, so the pair reads as a pair.
port: 7150
# Community stores must supply full URLs for icon and gallery — Umbrel's resolver
# only auto-prefixes URLs for the official umbrel-app-store.
icon: https://raw.githubusercontent.com/paulscode/umbrel-store/master/paulscode-knots-blake2b/icon.png
gallery:
  - https://raw.githubusercontent.com/paulscode/umbrel-store/master/paulscode-knots-blake2b/gallery/1.png
path: ""
defaultPassword: ""
releaseNotes: >-
  Updates the node to Bitcoin Knots 29.4.2.knots20260508, which carries the
  chain's temporary long coinbase maturity rule. Newly mined coins now need 6480
  confirmations before they can be spent, roughly 45 days at a ten minute block
  target, where the old wait was 100 blocks. The consensus rule begins being
  enforced at block 973440.


  This also affects coins you have already mined. Nodes require the longer wait
  of every coinbase spend they will relay, whatever height the coin was mined
  at, so a payout from the last 45 days that was spendable before this update
  will not be spendable after it. Nothing is lost; the wait is longer.


  Update before your node reaches block 973440 if you can. A node running this
  build from before that height checks the new rule as it goes and never has to
  look back.


  A node updated after it has passed 973440 has to rewind to that block and
  recheck everything since, which takes a while and needs the block data for
  that stretch still on disk. This app prunes, so that data does not stay
  forever. At the default pruning size there is plenty of room, but if you have
  set pruning low and update long after 973440, the node can refuse to start and
  report that required block data has been pruned. Recovering from that means
  downloading the chain again. Updating promptly avoids the question entirely.


  UPDATE ELECTRS PRUNED TOO, TO 0.11.12 OR LATER


  This node no longer reports a "difficulty" field for blocks with the new
  header format, which on this chain is every block above the activation. It
  reports "difficulty_blake2b" instead, because the two are different units and
  were never comparable.


  That is not only a display change, which is what these notes said at 1.1.9.
  Electrs Pruned treated the old field as mandatory and cannot start against
  this node without its own update: it loads its headers, then exits reporting
  "daemon not available", and the app shows as crashed. Nothing is wrong with
  your node or your index if you see that, and no reindex is needed. Update
  Electrs Pruned and it starts again.


  Mempool Pruned needed an update for the same reason and has one, at 3.3.1.7.

submitter: Paul Lamb
submission: https://github.com/paulscode/umbrel-store
widgets:
  - id: "stats"
    type: "four-stats"
    refresh: "5s"
    endpoint: "app:3000/api/widget/stats"
    link: ""
    example:
      type: "four-stats"
      link: ""
      items:
        - title: "Connections"
          text: "11"
          subtext: "peers"
        - title: "Mempool"
          text: "257"
          subtext: "MB"
        - title: "Hashrate"
          text: "590"
          subtext: "EH/s"
        - title: "Blockchain size"
          text: "5"
          subtext: "GB"
  - id: "sync"
    type: "text-with-progress"
    refresh: "2s"
    endpoint: "app:3000/api/widget/sync"
    link: ""
    example:
      type: "text-with-progress"
      link: ""
      title: "Blockchain sync"
      text: "83%"
      progressLabel: "In progress"
      progress: 0.83
backupIgnore:
  - data/blocks
  - data/chainstate
  - data/indexes
